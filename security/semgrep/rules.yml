rules:
  - id: python-no-eval
    languages: [ python ]
    message: "Avoid eval() — leads to code injection"
    severity: ERROR
    patterns:
      - pattern: eval($X)
    metadata:
      cwe: "CWE-95: Improper Neutralization of Directives in Dynamically Evaluated Code ('Eval Injection')"
